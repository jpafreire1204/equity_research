# Streamlit Cloud secrets template
# Copy to .streamlit/secrets.toml for local dev
# In Streamlit Cloud: paste these in Settings → Secrets

# Currently no secrets needed. The app runs on cached parquets in data/processed/
# Future placeholders below — uncomment when needed.

# [api_keys]
# anthropic_api_key = "sk-ant-..."
# exa_api_key = "..."
